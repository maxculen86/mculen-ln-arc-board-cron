import { anticipoBox as anticipoCommon } from '../../common/anticipoBox';

export const anticipoBox = (element, featureInfo) => {
    return anticipoCommon(element, featureInfo);
};

export default anticipoBox;
