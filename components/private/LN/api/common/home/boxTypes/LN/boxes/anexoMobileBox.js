import { anexoMobileBox as anexoBoxCommon } from '../../common/anexoMobileBox';

export const anexoMobileBox = (element, featureInfo, anexoFn) => {
    return anexoBoxCommon(element, featureInfo, anexoFn);
};

export default anexoMobileBox;
