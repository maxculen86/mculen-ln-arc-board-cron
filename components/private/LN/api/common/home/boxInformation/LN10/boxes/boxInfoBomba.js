import { boxInfoBasic } from '../../common/boxBasic';

export const boxInfoBomba = (information, section, typeSection) => {
    return boxInfoBasic(information, section, typeSection);
};

export default boxInfoBomba;
