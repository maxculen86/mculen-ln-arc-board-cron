import React from 'react';

export default function ({ onClick }) {
    return (
        <button
            role="btn-face"
            className="icon-facebook"
            id="pie-facebook"
            onClick={onClick}
        />
    );
}
