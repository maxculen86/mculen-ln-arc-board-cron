'use strict';

import React from 'react';

export default ({ onClick }) => {
    return (
        <button
            className={'icon-facebook'}
            id="pie-facebook"
            onClick={onClick}
        />
    );
};
