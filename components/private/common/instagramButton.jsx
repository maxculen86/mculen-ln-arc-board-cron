'use strict';

import React from 'react';

export default ({ onClick }) => {
    return (
        <button
            className="icon-instagram"
            id="pie-instagram"
            onClick={onClick}
        />
    );
};
