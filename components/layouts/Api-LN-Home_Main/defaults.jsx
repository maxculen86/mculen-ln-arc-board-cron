import React from 'react';

const ApiLNHomeMain = ({ children }) => {
    return (
        <div>
            {/* Contenido */}
            {children}
        </div>
    );
};

export default ApiLNHomeMain;
