// Subtypes de oembed que se renderizan como un <iframe> embebido.
export const EMBED_SUBTYPES_WITH_IFRAME = ['youtube', 'dailymotion', 'vimeo'];

// Matchea la etiqueta de apertura del <iframe>.
export const IFRAME_REGEX = /<iframe\b[^>]*>/i;
