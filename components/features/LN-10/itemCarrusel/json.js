import getItemCarrusel from '../../../private/LN/api/global/components/chains/LN10/getItemCarrusel';

class ItemCarrusel extends getItemCarrusel {
    constructor(props) {
        super(props, { variant: 'default' });
    }
}

export default ItemCarrusel;