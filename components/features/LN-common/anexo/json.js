import Consumer from 'fusion:consumer';

class AnexoFeature {
    constructor(props) {
        this.props = props;
    }

    render() {
        return {};
    }
}

export default Consumer(AnexoFeature);
