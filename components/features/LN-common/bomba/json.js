import Consumer from 'fusion:consumer';

class BombaFeature {
    constructor(props) {
        this.props = props;
    }

    render() {
        return {};
    }
}

export default Consumer(BombaFeature);
